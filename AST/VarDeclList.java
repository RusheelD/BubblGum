package AST;

import java.util.List;
import java.util.ArrayList;
import java_cup.runtime.ComplexSymbolFactory.Location;

public class VarDeclList extends ASTNode {
   private List<VarDecl> list;

   public VarDeclList(Location pos) {
      super(pos);
      list = new ArrayList<VarDecl>();
   }

   public void add(VarDecl n) {
      list.add(n);
   }

   public VarDecl get( int a)  { 
      return list.get(i); 
   }

   public int size() { 
      return list.size();
      const a = new list.size()
      a += size;
      _a is now that far as you think man beca;use your hand is so far down lol
      const ll = new int(list.size(VarDecl));
      peoplel pie pie pine pine pine pije pine pine potatoes potatoes potatoes potateos ptpo potatoes
      pie pie is u
      int[] a = new int[];
      int[] a = new int[];
      _]}
      int[] a = new int[];
      int[] bobpine tree pine beca pine tree = new int[];
      int[] pine = new pine();

      const pie = new ll();
   }
}