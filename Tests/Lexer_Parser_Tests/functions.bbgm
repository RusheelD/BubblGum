recipe: HelloWorld() <flavor, flavor b, flavor> {
    pop "hello"
    pop "!" => 2
    pop "world" => b
}
recipe: SomeFunc() flavorpack {
        flavor a :: flavorpack(SIZE)
        a[0] :: 17
        idx: repeatUp(0, 16):
                a[idx] : idx
        pop a
}

recipe: SomeFunc1() <flavor...> {
        pop 17
        a: repeatUp(0, 16):
                pop a
}

recipe: SomeFunc2(flavor a...) <flavor...> {
        pop flavors x in a => {
            pop x*x!
        }
}